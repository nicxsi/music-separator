apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090    # The service name inside docker compose
    isDefault: true
    editable: false
